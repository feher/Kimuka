<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_done"
        android:title="@string/add_availability_done"
        android:icon="@drawable/ic_done"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_send_request"
        android:title="@string/add_availability_send_request"
        android:icon="@drawable/ic_group_add"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_cancel"
        android:title="@string/add_availability_cancel"
        android:icon="@drawable/ic_group_add"
        app:showAsAction="always"/>

</menu>
